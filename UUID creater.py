from models import Tickets,Users,Products, Orders
from uuid import uuid4
from datetime import datetime

#     !Создание новых Тикетов!
# for i in range(47):
#     new_uuid: str = str(uuid4())
#     Tickets.create(uuid=new_uuid, available=True)


#     !Удаление Тикетов под индексом!
# Tickets.get(Tickets.id==18).delete_instance()






